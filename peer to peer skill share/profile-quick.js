document.addEventListener('DOMContentLoaded', () => { const targets = ['.sidebar .profile', '.topbar .profile', 'header .profile', '.header .profile']; document.querySelectorAll(targets.join(',')).forEach(item => { if (item.closest('.profile-popover') || item.dataset.profileLinked) return; item.dataset.profileLinked = 'true'; item.setAttribute('title', 'Open your profile'); item.style.cursor = 'pointer'; item.addEventListener('click', event => { if (event.target.closest('button.icon-btn') || event.target.closest('a')) return; window.location.href = 'profile.html' }); item.addEventListener('keydown', event => { if (event.key === 'Enter') window.location.href = 'profile.html' }); if (!['BUTTON', 'A'].includes(item.tagName)) item.tabIndex = 0 }) });
