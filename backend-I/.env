# ==========================================
# SkillShare Backend — Environment Variables
# Copy this file to ".env" and fill in REAL values locally.
# NEVER commit the real .env file to Git.
# =============cmd /c "echo.| call start-skillshare.bat"=============================

#cmd /c "cd /d c:\project p2p && start-skillshare.bat"         greSQL connection string (local or Neon).
# Local example:      postgresql://postgres:YOUR_LOCAL_PASSWORD@localhost:5432/skillshare
# Neon: hostname resolved by IP + endpoint option (local DNS fails for *.neon.tech).
DATABASE_URL=postgresql://neondb_owner:npg_ahdkLCVYl39S@3.23.186.13/neondb?sslmode=require&options=endpoint%3Dep-lingering-firefly-a5ni2e23

# JWT signing secret. Generate one with:  python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=Z01MFjS9o3bYOAYXvzaFo1doIHSP_0xPnUxw5aa2dPfPQeeE0IsnOFRAiP3plqqW

# JWT algorithm
ALGORITHM=HS256

# Access token lifetime in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Allowed frontend origins for CORS (comma-separated). Used in production (e.g. Vercel URL).
# Leave empty for local development (localhost origins are always allowed).
FRONTEND_URL=http://localhost:5500

# LiveKit Cloud — real-time audio/video (server-side ONLY).
# SECURITY: never put the secret in HTML/JS or commit .env to Git.
LIVEKIT_URL=wss://academia-1wol1wxe.livekit.cloud
LIVEKIT_API_KEY=APIuuBYWvv3jyot
LIVEKIT_API_SECRET=erlIMGRkB3wsoogVPzfdGCD2EKZhPEvaULEnze0Tpn3A
